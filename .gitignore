node_modules
dist
dev-dist
coverage
*.local
.DS_Store

# Capacitor native projects are generated with `npx cap add <platform>`;
# commit them once real native builds start.
# native projects are tracked (Capacitor); their own .gitignores skip builds
dist-artifact
