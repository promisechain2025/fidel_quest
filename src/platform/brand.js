/* The name a child sees. Ge'ez in every UI language — English, Amharic,
   Tigrinya, and the diaspora packs. Latin "eGeez" stays in store copy and
   technical docs, not on screen. */
export const APP_NAME = 'ኢግእዝ'

/* The hyena's kid-facing name, in every UI language. Code identifiers
   stay (JibbySvg, drawHyena). Latin "Jibby" / "Hyena" is not a label. */
export const HYENA_NAME = 'ጅብ'
